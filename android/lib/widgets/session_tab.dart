import 'package:flutter/material.dart';

class SessionTabWidget extends StatelessWidget {
  const SessionTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Session Management View'),
    );
  }
}
